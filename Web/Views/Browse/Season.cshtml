@using Core.Models.Classes
@model Season

@{
    Layout = "~/Views/Shared/_Default.cshtml";
}

@section Title
{
    Browse &gt; @Model.AdministrativeBody.Name &gt; @Model.Name
}

<div class="row">
    <div class="col-md-12">
        <ol class="breadcrumb">
            <li><a href="@Url.Action("Index", "Browse")">Browse</a></li>
            <li><a href="@Url.Action("AdministrativeBody", "Browse", new { id = @Model.AdministrativeBody.Id })">@Model.AdministrativeBody.Name</a></li>
            <li class="active">@Model.Name</li>
        </ol>
    </div>
</div>
<div class="row">
    @foreach (Division division in Model.Divisions.OrderBy(x => x.Sport).ThenBy(x => x.ParticipationType).ThenBy(x => x.CompetitionFormat).Take(10))
    {
        <div class="col-sm-6 col-md-4">
            <div class="thumbnail">
                <img src="http://www.u3ajavea.org/Activity-Groups/Snooker/images/snooker.jpg" alt="@division.Name">
                <div class="caption">
                    <h3>@division.Name</h3>
                    <p>@division.ParticipationType @division.Sport @division.CompetitionFormat</p>
                    <p>
                        <a href="@Url.Action("Division", "Browse", new { id = division.Id })" class="btn btn-primary" role="button">View</a>
                    </p>
                </div>
            </div>
        </div>
    }
</div>